export enum OrderStatus {
  Completed = 'Completed',
  Waiting = 'Waiting',
  TimeOut = 'TimeOut',
  Failed = 'Failed',
  Closed = 'Closed',
}

export enum BalanceType {
  Recharge = 1,
  Consume = 2,
  Initial = 3,
}
