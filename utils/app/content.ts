// import { GPTContent, LingJiContent } from '@/types/chat';

// export function isGPTContent(
//   object: GPTContent[] | LingJiContent[] | undefined | string
// ) {
//   return typeof object === 'object' && object !== null && 'type' in object;
// }

// export function isLingJiContent(
//   object: GPTContent[] | LingJiContent[] | undefined | string
// ) {
//   return typeof object === 'object' && object !== null && 'image' in object;
// }

// export function getContentText(content: string) {
//   try {
//     JSON.parse(content);
//   } catch (error) {
//   } finally {
//   }
// }
